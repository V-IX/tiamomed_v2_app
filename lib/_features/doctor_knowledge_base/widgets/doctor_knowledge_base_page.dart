import 'package:flutter/material.dart';

class DoctorKnowledgeBasePage extends StatelessWidget {
  const DoctorKnowledgeBasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Doctor Knowledge Base Page'),
      ),
      body: const Center(
        child: Text('Doctor Knowledge Base Page'),
      ),
    );
  }
}
